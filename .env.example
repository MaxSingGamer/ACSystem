# ============================================================
#  AEU 账户配置模板（v3.0.0：各端独立数据目录）
#  服务端：复制本文件到 ~/.alpha_dir/acs-server/.env 并按需修改。
#  （客户端/其他端各自目录：~/.alpha_dir/acs-client/.env 等）
# ============================================================
#  格式：
#    ACS_ADMIN_ACCOUNTS = uid:role:初始密码  （多个用逗号分隔；role=root/finance）
#    ACS_SYSTEM_ACCOUNTS = uid:初始密码      （多个用逗号分隔）
#    PRE_ISSUED_ACCOUNT  = 发行账户 uid      （只能一个，且必须为 ACS_SYSTEM_ACCOUNTS 之一）
#
#  说明：
#    - 存在 <数据目录>/.env 时：自动禁用默认 admin；
#      管理员/系统账户按下方定义创建，密码只存哈希、不输出 txt。
#    - 不存在时：默认创建 admin（随机密码输出到 SYSTEM_LOGIN_PASSWORDS.txt），
#      且不创建系统账户。
#    - 配置了 ACS_SYSTEM_ACCOUNTS 时必须同时指定 PRE_ISSUED_ACCOUNT（且唯一、
#      且在系统中），否则服务端拒绝启动并记录 .alphalog 错误。
#    - 初始密码仅用于首次登录/首次创建，登录后请立即修改。
#    - 密码不要包含冒号 ":" 或逗号 ","。
# ============================================================

# ============================================================
#  网络 / 监听（均可选：保持注释即用默认值）
# ------------------------------------------------------------
#  数据目录：数据库 / gpg / 系统账户密钥 / 本 .env 的存放位置
#    默认 ~/.alpha_dir/acs-server
# ACS_DATA_DIR=C:\Users\<你的用户名>\.alpha_dir\acs-server
#
#  公开 API：客户端访问（ed25519 签名校验 · 同步免 apikey）
#    默认监听 0.0.0.0:9600；对外只需暴露此端口
# ACS_PUBLIC_BIND=0.0.0.0
# ACS_PUBLIC_PORT=9600
#
#  后台管理：网页后台 + 管理 API（/login /root /finance /sys）
#    默认仅本机 127.0.0.1:9680；改为 0.0.0.0 即开放公网
#    （开放公网务必使用强口令、限制来源 IP，并关注审计日志）
# ACS_ADMIN_BIND=127.0.0.1
# ACS_ADMIN_PORT=9680
#
#  网页后台静态资源目录（一般无需设置；默认取可执行文件同级 static/）
# ACS_STATIC_DIR=D:\ACS\Server\static
# ============================================================

ACS_ADMIN_ACCOUNTS=Max_Shin:root:ChangeMe123!,Mitrailleur_2905:root:ChangeMe123!

ACS_SYSTEM_ACCOUNTS=PreIssuedAccount:ChangeMe123!,AESystem:ChangeMe123!,AlphaEU:ChangeMe123!

PRE_ISSUED_ACCOUNT=PreIssuedAccount
